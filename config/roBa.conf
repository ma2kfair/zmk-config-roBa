# マウス機能を有効化（&mmv, &msc, &mkp を使うために必須）
CONFIG_ZMK_MOUSE=y

# 接続の安定性を高める（任意ですが推奨）
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# ※ SMOOTH_SCROLLING は新しい機能ですが、古い環境や特定のOSではこれが y だと逆に動かないことがあるため、あえて n に
CONFIG_ZMK_MOUSE_SMOOTH_SCROLLING=n
